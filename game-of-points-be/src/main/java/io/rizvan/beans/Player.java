package io.rizvan.beans;

public class Player extends CompetingEntity {
    public Player(int hitPoints, double x, double y, double speed, double points) {
        super(hitPoints, x, y, speed, points);
    }
}
